{
  "name": "flashcard",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "start": "cd root169 && npm run build",
    "prebuild": "cd backend && npm install && cd ../root169 && npm install",
    "build": "cd backend && npm start",
    "postbuild": "echo 'Build completed' && cp -r root169/build backend/build",
    "serve": "serve -s backend/build"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "engines": {
    "node": ">=18"
  },
  "devDependencies": {
    "serve": "^14.0.1"
  }
}
