{
  "name": "flashcard",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "start": "cd backend && npm start",
    "prebuild": "cd backend && npm install && cd root169 && npm install",
    "build": "cd root169 && npm run build",
    "postbuild": "mv -f root169/build backend",
    "serve": "serve -s backend/build"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "engines": {
    "node": ">=18"
  },
  "devDependencies": {
    "serve": "^14.0.1"
  }
}
